{ is_figure_t.pas }

function IsFigureTTop(var sample: TMatrix): boolean;
begin
  IsFigureTTop := True; { Заглушка. }
end;

function IsFigureTRight(var sample: TMatrix): boolean;
begin
  IsFigureTRight := True; { Заглушка. }
end;

function IsFigureTDown(var sample: TMatrix): boolean;
begin
  IsFigureTDown := True; { Заглушка. }
end;

function IsFigureTLeft(var sample: TMatrix): boolean;
begin
  IsFigureTLeft := True; { Заглушка. }
end;
