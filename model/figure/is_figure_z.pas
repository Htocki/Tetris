{ is_figure_z.pas }
function IsFigureZTop(var sample: TMatrix): boolean;
begin
  IsFigureZTop := True; { Заглушка. }
end;

function IsFigureZRight(var sample: TMatrix): boolean;
begin
  IsFigureZRight := True; { Заглушка. }
end;

function IsFigureZDown(var sample: TMatrix): boolean;
begin
  IsFigureZDown := True; { Заглушка. }
end;

function IsFigureZLeft(var sample: TMatrix): boolean;
begin
  IsFigureZLeft := True; { Заглушка. }
end;
